# Pendientes

## Taller 1 (`src/uno/main.cpp`)

- [ ] Eliminar el comentario `// DEBUG TEMPORAL: valor crudo de A0 para diagnosticar el pot rojo`
      sobre las líneas `analogWrite`. Quedó huérfano: el `Serial.println` de debug que
      describía ya no está ahí, así que el comentario no aplica a esas líneas.
